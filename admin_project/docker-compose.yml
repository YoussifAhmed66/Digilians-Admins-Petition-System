version: '3.8'

services:
  admin-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: admin_backend
    ports:
      - "8001:8080"
    env_file:
      - .env
    restart: unless-stopped
